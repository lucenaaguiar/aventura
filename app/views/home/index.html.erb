<%= link_to 'Entrar', new_guide_session_path %>
<section>
  <%= @tours.each do |tour| %>
    <div>
      <h1><%= link_to tour.title, tour %></h1>
      <dl>
        <dt>Título</dt>
        <dd><%= tour.location%></dd>
        <dt>Categoria</dt>
        <dd><%= tour.category.name %></dd>
      </dl>
    </div>
  <% end %>
</section>
